{
	"foo": {
		"simple string in map": "foo",
		"multiline string in map": "foo\nbar",
		"list in map": [
			"simple string in list",
			"multiline string\nin list",
			{
				"now": "map",
				"in": "list"
			},
			[ "list", "in", "list" ]
		],
		"map in map": {
			"in": "map",
			"on": "map"
		}
	}
}
